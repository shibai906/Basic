备忘录模式：
    就是这一点：当我们使用文本编译器记录一些内容时，发现这一步写错了，可以使用后退键的方式退到上一个状态，就是用的这一原理。主要就是为了保存某个对象内部状态的拷贝。一般使用栈的方式进行备忘就行


模式结构
1、发起者角色：负责创建一个备忘录用以记录当前时刻它的内部状态，并可以使用备忘录恢复内部状态。
2、备忘录角色：负责存储发起者对象的内部状态，并可以防止发起者角色以外的其他对象访问备忘录。
3、管理者角色：负责保存好备忘录。









